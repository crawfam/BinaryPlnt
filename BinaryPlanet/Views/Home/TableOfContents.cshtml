@model List<BinaryPlanet.Models.Poem>

@{
    ViewBag.Title = "Table of Contents";
}

@section Styles {


    <title>@ViewBag.Title</title>

}

<!-- LINK LIST - SECTION -->
<section class="slice sct-color-2 bb">
    <div class="container">
        <div class="row">
            <h1 style="text-align:center;" class="parallax-title c-white">Table of Contents</h1>            
            <div class="col-md-3">
                <h1 class="parallax-title c-white">0001</h1>


                @foreach (BinaryPlanet.Models.Poem p in Model.Where(s => s.Section == 1))
                {
                    <div class="block style-5 v3">
                        <div class="block-icon">
                            <i class="fa fa-check-circle"></i>
                        </div>
                        <div class="block-content">
                            <a href="@Url.Action("Poem", "Poems", new {Id = p.Id})" class="block-title-2">@p.Name</a>
                            <p>
                                @p.TagLine
                            </p>
                        </div>
                    </div>
                }

            </div> <!-- End of Section 0001 -->
            <!-- SECTION 0010 -->

            <div class="col-md-3">
                <h1 class="parallax-title c-white">0010</h1>

                @foreach (BinaryPlanet.Models.Poem p in Model.Where(s => s.Section == 2))
                {
                    <div class="block style-5 v3">
                        <div class="block-icon">
                            <i class="fa fa-check-circle"></i>
                        </div>
                        <div class="block-content">
                            <a href="@Url.Action("Poem", "Poems", new {Id = p.Id})" class="block-title-2">@p.Name</a>
                            <p>
                                @p.TagLine
                            </p>
                        </div>
                    </div>
                }

            </div> <!-- END OF SECTION 0010  -->

            <div class="col-md-3">
                <h1 class="parallax-title c-white">0011</h1>

                @foreach (BinaryPlanet.Models.Poem p in Model.Where(s => s.Section == 3))
                {
                    <div class="block style-5 v3">
                        <div class="block-icon">
                            <i class="fa fa-check-circle"></i>
                        </div>
                        <div class="block-content">
                            <a href="@Url.Action("Poem", "Poems", new {Id = p.Id})" class="block-title-2">@p.Name</a>
                            <p>
                                @p.TagLine
                            </p>
                        </div>
                    </div>
                }


            </div>

            <!-- END OF SECTION 0011 -->

            <div class="col-md-3">
                <h1 class="parallax-title c-white">0100</h1>

                @foreach (BinaryPlanet.Models.Poem p in Model.Where(s => s.Section == 4))
                {
                    <div class="block style-5 v3">
                        <div class="block-icon">
                            <i class="fa fa-check-circle"></i>
                        </div>
                        <div class="block-content">
                            <a href="@Url.Action("Poem", "Poems", new {Id = p.Id})" class="block-title-2">@p.Name</a>
                            <p>
                                @p.TagLine
                            </p>
                        </div>
                    </div>
                }

            </div>

        </div>
    </div>

</section>

